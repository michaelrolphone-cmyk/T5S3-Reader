#pragma once

#include <T5TimecardApi.h>

// Implementation unit for the firmware-backed native Timecard service.
// The exported symbol is declared in T5TimecardApi.h and registered with the
// ELF loader alongside the generic native-app API.
