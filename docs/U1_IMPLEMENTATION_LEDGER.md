# U1 implementation ledger

[PR #96](https://github.com/michaelrolphone-cmyk/T5S3-Reader/pull/96), `impl/u1-riscrte`, is the **only** U1 implementation branch/PR. [USB remediation](USB_CONTRACT_VIOLATION_REMEDIATION.md), [execution order](FOUR_MILESTONE_STREAM_FIRST_EXECUTION_ORDER.md), [claim-scoped USB control](U1_USB_CONTROL_SCOPE_IMPLEMENTATION.md), [package identity](PACKAGE_IDENTITY_VERSION_POLICY.md), and `AGENTS.md` remain binding. Committed code does not establish a build/test PASS. Owner controls merge, tag, release, flash and physical qualification.

## Ancestry and versions

Last actual comparison on September 21 at `6e0a52b`: master `839f66c` had **119 master-only commits** and U1 had 283 branch-only then; further U1 commits followed. Last full master backmerge `1ebfbfe` at `0a8c21c`; master #103 CH34x and #105 DMA/VBUS were selectively integrated, **not fully backmerged**. Master firmware was 1.2.48 at that comparison; U1 firmware 1.2.35 remains unreleased. U1 apps: Driver Manager 1.0.5, App Store 1.0.3, Package Manager 1.0.1. Current driver manifests: board-power 0.1.6, USB controller 0.1.5, host 0.1.4, **CDC `usb-cdc-acm-v2` 0.1.2 -> 0.1.3 (`c846628`)**, **CP210x 0.1.3 -> 0.1.4 (`ad38c6f`)**, **CH34x 0.1.2 -> 0.1.3 (`d62ace1`)**. The forked CDC ID remains a known defect, not an accepted canonical migration. CH34x status remains experimental-unpublished. No release/version publishing occurred.

## Previous committed implementation

- Generic ELF byte/record streams, execution-context-owned leases/revocation and partial RX/TX/backpressure. Recoverable four-kind per-ID SD/ZIP package engine, bounded bootstrap, immutable SHA-pinned catalog, shared App Store/Driver Manager/Package Manager and manifest-driven exporter. Prior isolated mutation sanitizer smoke is not current-head evidence.
- Installed `i2c.bus` ELF is the ONLY authorized temporary bounded raw firmware I²C importer pending U3. Installed `board.power.vbus` ELF owns BQ25896 charger/OTG/VBUS/telemetry and BATFET shutdown, with UI/sleep grants and SD/startup ordering. BQ27220 is separate. No REG12 charger-current ADC measurement is claimed; physical behavior unqualified.
- `02ff428`–`fd94430`: host checked release and CDC/CP210x session/orphan retention. `ca2c26f`–`5d9b5b5`: controller two-stage interface/device close and detached reap. `db75070`–`6b7b41b`: master #105 bounded 500-ms bulk DMA halt/flush/callback drain, IDF/VBUS shutdown ordering and uncertain-cleanup quarantine. Historic isolated sanitizer smoke is NOT target ELF evidence.
- `d534030`–`f63c2f8`: independent CH34x class ELF with actual WCH matching/vendor requests, baud/framing, DTR/RTS and bulk transfers. `5eaa653`–`9d81322`: CP210x confirmed detach versus unknown-state checked close. `9e56af0`–`9c5790e`: host's legacy device-token control rejects; append-only claim-scoped control isolates interface and sole-claim device vendor commands. All three class ELFs use their own claims.
- Through `bcd89c1`: `efadf13` appends `risc_usb_host_snapshot_v1`; `b308b86` implements installed-host bounded event/identity snapshot, transient descriptor failures distinct from detach; `23defff` changes firmware reconcile to consume that snapshot rather than directly poll/list/configure USB and retries checked class/host cleanup after quarantine; `b2e0c51`, `ff62bba`, `bab09c1` add snapshot/teardown regressions and runner; `5942bc9` bumps host 0.1.4. Compiled legacy selection/presentation remain.

## September 21: generic publication and provider selection

- `f7d8a90`, `e15799c`: generic transport-neutral generation-checked semantic publisher; rejects stale identity and conflicting metadata, checked withdrawal. `6fa11df` routes transitional firmware USB serial projection through publisher. `e1a685a`, `9498bcb` tests/runner. **Publication is not yet provider-originated.**
- `352fd72`, `affff67`: generic serial registry retains exact private lease on malformed/failed acquisition with uncertain physical release; no public lease or streams escape; context shutdown and next acquisition retry same release and deny reconnect until success. `c1335b8`, `6e0a52b`, `da5147e` tests/runner.
- `0504f3d`, `990884a`: hardware-blind installed-manifest capability/API candidate enumeration, exact grants and checked rejected-candidate release. `8b1ae48` exposes unbound retained class grant to checked compatibility stop. `594d63c`, `952a56e`, `5f9b959` tests/runner; `59deb28` prior ledger update. Class matching then still occurred through ELF `open()`.

## September 21: retained grants and production host/class integration

- `044bc89`: `InstalledProviderGraph::acquire()` preserves exact generation-bound grant if interface is absent and checked release fails. `a31dd84`, `3007746`: generic `InstalledProviderSession` owns selected grant/rejection and fault quarantine; compiled class adapter delegates grant ownership to it. `aaee82f`, `c7603d5`: session regression/runner. `fa9f579`, corrected by `9eb5411`: a failed host acquire cannot overwrite an exact retained grant; compatibility stop retries same checked grant, while an already-running session can reconfigure.
- `3026b38`, `6473517`: production USB host + CDC/CP210x/CH34x ELF integration test against simulated physical controller wired into pre-existing USB host runner. Covers real class parsing, claimed controls, line coding, DTR/RTS, partial bulk I/O, failed physical release and reconnect generations. Committed, **not observed passing**. `0d94ef9` previously updated ledger/PR.

## September 21 continuation: targeted grantless recovery and ELF-owned probes

- `64161ec` adds `GraphV2::recoverFailedFrom`: selects exactly the faulted provider identity/capability/API, rejects recovery with live/pending grants or consumers, calls its checked `ModuleV2::unload()` and releases dependency pins **only after verified quiescence**; does not run global shutdown or revoke other providers. `bb0ed5f`, `a720d6e` expose and implement this route in `InstalledProviderGraph`.
- `56b8513`, `b815c94` retain the failing candidate's exact installed ID on grantless activation failure in the hardware-blind selector/session and retry this targeted graph recovery; `112950c` bounds the copied capability name. Failure retains quarantine and bars cross-class selection. `49bb4f8` extends selected-session fault/retry regression. `3b2bc38`, `557d1d4` add a **production GraphV2 + mapped ELF fixture** targeted-recovery test to existing CI runner: an unrelated provider holds a live grant on the same dependency while the failed one is repaired. No test PASS was observed.
- `a50acc1` appends `risc_usb_serial_class_discovery_v1` without changing the existing `serial.port@1` ABI prefix. `probe(device)` must be read-only and return match/nonmatch/uncertain. `c48fc65`, `9f76033`, `3f78b60` implement side-effect-free descriptor matching inside the CDC, CP210x and CH34x ELFs respectively; all chipset VID/PID and descriptor policies remain in class ELFs, not firmware. Probe performs **no physical claim, vendor control or session open**. Negative descriptor failure cannot be treated as an ordinary nonmatch.
- `90f7e75` connects installed-class selection to the ELF probe, requiring the extension for an explicitly identified physical device; a negative probe faults/quarantines selection. `5394b4e` makes compiled transitional `NativeUsbBridge::openClass` present the host's exact generation token **before every candidate probe**, including after unbinding/rejecting an earlier class. This remains a compiled compatibility loop and is not the full architectural cutover.
- `c846628`, `ad38c6f`, `d62ace1`: class package version increments recorded above. `93009f9` updates source-boundary regression for actual generic session and ELF-owned probes; `f15918a`, `b5784b4`, `b6200bf` update the production CDC/CP210x/CH34x tests for append-only ABI, match/nonmatch, descriptor uncertainty and side-effect checks while retaining protocol/teardown cases. `710913a` extends generic selector regression with a fourth manifest-discovered class, but this is a **synthetic fourth candidate**, not a fourth packaged hardware ELF/end-to-end acceptance.

## Observed evidence and incomplete acceptance

Historical successful workflow `35427506383` executed at older `7bffec3`, **not this head**. GitHub lookup at `710913a` returned **zero PR workflow runs and zero combined statuses**; no present-head PASS is claimed. Container GitHub DNS resolution failed, preventing full checkout and current-head local firmware/Xtensa ELF/app/package/catalog/import/relocation builds. No new test (including the production host/class integration, probe, fourth-candidate, or graph recovery tests) was observed passing. No merge, tag, release, flash, owner hardware qualification, manual-approval workflow or second implementation PR.

1. **Primary U1 blocker:** remove the compiled class/device selection and binding loop and move provider-originated `serial.port` semantic device/stream publication out of `NativeUsbBridge`, `NativeUsbClassBridge`, `NativeSerialPortBridge_implementation.inc` and USB-specific registry. Installed ELFs now own *read-only matching*, class protocols and physical claims; the compiled compatibility bridge **still** initiates probing, session binding, registry state and data shuttles. Fully generic runtime must manage opaque selection/leases/streams and consumer path without VID/driver lists or hardware-specific firmware helpers; prove newly installed fourth class from package to publication, Serial Monitor/programmer DTR/RTS and backpressured reconnect I/O.
2. Reconcile remaining master commits safely; implement exclusive internal USB/debug-console PHY handoff, restore and quarantine on uncertain release.
3. Replace parsed `USBREF`/`VBUSREF`/`USBCTRL` logs with structured provider errors; safely migrate `usb-cdc-acm-v2` to canonical `usb-cdc-acm` and retire legacy proxy.
4. Remove signing/P-256/floor while retaining SHA/TLS/ABI/import validation/rollback; finish nested resources and generation-bound ELF verification receipts.
5. Obtain single-head actual firmware, Xtensa providers, applications, imports/relocations, packages/catalog, missing/corrupt installs and charging/OTG/sleep test evidence. Hardware qualification is owner-directed after software completion. U3 owns native I²C/SPI/UART and unrelated peripherals; U4 owns CAM provisioning.

**Next production work:** provider-originated class activation and semantic publication through generic runtime, not another compiled USB dispatcher. **Implementation In Progress.**
