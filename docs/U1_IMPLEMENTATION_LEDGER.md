# U1 implementation ledger

[PR #96](https://github.com/michaelrolphone-cmyk/T5S3-Reader/pull/96), `impl/u1-riscrte`, is the sole U1 implementation PR. Distinguish **committed source, observed tests and incomplete acceptance**. [USB remediation](USB_CONTRACT_VIOLATION_REMEDIATION.md), [execution order](FOUR_MILESTONE_STREAM_FIRST_EXECUTION_ORDER.md), [claim-scoped USB control](U1_USB_CONTROL_SCOPE_IMPLEMENTATION.md), [package identity policy](PACKAGE_IDENTITY_VERSION_POLICY.md), and `AGENTS.md` remain binding. The owner authorizes merge, tag, release, flashing and physical qualification.

## Ancestry and versions

U1 last backmerged master `1ebfbfe` at `0a8c21c`. **September 21 actual comparison** at `6e0a52b`: master `839f66c` (firmware 1.2.48), **119 master-only and 283 U1-only commits**. Master #103 CH34x and #105 DMA/VBUS were selectively integrated, **not fully backmerged**. Reconcile without discarding changes. U1 firmware 1.2.35 unreleased. App versions: Driver Manager 1.0.5, App Store 1.0.3, Package Manager 1.0.1. Manifest versions: board-power 0.1.6, controller 0.1.5, usb-host-v2 0.1.4, CDC 0.1.2, CP210x 0.1.3, CH34x 0.1.2. CDC forked `-v2` identity still requires explicit safe migration; a version bump is not identity migration. This continuation changes only compiled generic runtime code, compatibility adapter and host tests: no ELF/package manifest version was changed.

## Generic runtime, packages and actual hardware ownership

- Generic ELF byte/record streams, context-owned grants/revocation, bounded partial RX/TX/backpressure, provider discovery and API-aware dependencies are committed. USB stop releases only class/host grants, never global graph shutdown. Failed physical release retains exact mapped generations and teardown obligations.
- Only installed `i2c.bus` ELF may temporarily import bounded firmware raw I²C; native takeover belongs to U3. Installed BQ25896 `board.power.vbus` ELF owns charger/OTG/telemetry and one-way BATFET shutdown; board UI/sleep use verified grants, defer charger config until SD mount and reserve sleep ELF before SD shutdown. BQ27220 gauge separate. No second normal firmware BQ writer or absent-package fallback. BQ snapshot does not measure REG12 charger-current ADC. Hardware unverified.
- Four-kind common package engine has recoverable per-ID SD/ZIP install, bounded bootstrap, SHA-pinned immutable catalog, shared App Store/Package Manager/Driver Manager operations, manifest-driven exporter and API-aware dependencies. Earlier isolated cross-translation-unit mutation ASan/UBSan smoke passed; no present-head build/rollback/corrupt-package acceptance verified.

## Physical USB/controller/class source evolution

- `02ff428`–`fd94430`: append-only host `release_checked`, CDC/CP210x exact-session retention and bounded failed-open orphan cleanup. Closing claims deny I/O and remain retryable; current-head tests unverified.
- `ca2c26f`–`5d9b5b5`: physical controller two-stage interface/device-close recovery and detached-handle reaping; earlier isolated release-policy C++17 sanitizer smoke passed, not production linkage. `db75070`–`6b7b41b` selectively integrated master #105 bounded 500-ms bulk halt/flush/callback drain and ordering DMA/interface/IDF host/VBUS cleanup plus final IDF NO_CLIENTS event; uncertainty pins provider/rail. Controller 0.1.5 target-link/hardware unverified.
- `d534030`–`f63c2f8`: master #103 CH34x independently installable serial class, WCH VID/PID/descriptor/version/init, framing, inverted DTR/RTS and bulk I/O inside ELF; checked release/failed-open orphan retry. Distinct from CDC identity fork.
- `5eaa653`–`9d81322`: CP210x refreshes host inventory before UART disable; confirmed detach skips impossible controls and releases same claim; unknown state/attached NACK/release failure pin and retry.
- `e4c6739`–`cd8dc9a` initial host gate allowed class-crossing legacy device control. `9e56af0`–`9c5790e` corrected: legacy host.control rejects, `control_claim` validates active nonclosing claim and present device, interface-recipient index and exclusive device-recipient access. CDC/CP210x/CH34x use own claims. Production host/composite tests committed; no current-head PASS. [Detailed contract](U1_USB_CONTROL_SCOPE_IMPLEMENTATION.md).

## Host-owned snapshot and reconnect cleanup (previous continuation)

- `efadf13` appends `risc_usb_device_identity_v1`/`risc_usb_host_snapshot_v1` to unchanged host/discovery ABI; presence and identification distinct. `b308b86` implements USB host ELF snapshot: at most 16 events, bounded generation-safe present device tokens/VID/PID, identified=0 on transient descriptor failure rather than fake detach, no partial records on capacity failure, event loss fails closed.
- `23defff` replaces firmware `NativeUsbBridge::reconcile` poll/device-list/4-KiB descriptor retrieval with provider snapshot. **Class selection and compatibility serial state are still firmware-side.** It also lets `serialStop` retry same checked close and host grant release after quarantine; only successful both clears quarantine.
- `b2e0c51` production host snapshot regression, `ff62bba` test runner integration, `bab09c1` source regression, `5942bc9` host manifest 0.1.4. These commits/tests are NOT observed green at current head.

## September 21 continuation: semantic publication and failed-acquire ownership

**Production code committed:** `f7d8a90` introduced generic `ProviderDevicePublisher` (transport-neutral descriptor, generation-bound publish/withdraw, registry lease revocation); `e15799c` rejects stale-generation resurrection and conflicting same-generation metadata without revoking live leases. `6fa11df` connects the existing *transitional* `UsbSerialProjection` to this common publisher rather than duplicating direct registry add/remove/lifetime logic. This is reusable generic publication, **not provider-originated USB announcements**; firmware still invokes the adapter, manages host snapshots/class selection, uses its USB-specific device registry and stream shuttle. Never report full USB extraction for these changes.

**Ownership fix:** `352fd72` makes `RuntimeSerial::Registry` retain a private lease if a failed or malformed provider acquisition returns an exact token and its checked release fails. The app receives no invalid public handle/stream, other acquisitions cannot displace the provider, and `end()` retries the retained token. `affff67` additionally retries the SAME orphan release once at a subsequent acquisition before admitting a reconnect; failure remains quarantined, success permits fresh acquisition. Existing successful lease release semantics remain unchanged. This is generic serial lifecycle code, not chipset-specific firmware logic.

**Tests committed:** `e1a685a` generation/lease publisher regression, `9498bcb` runner wiring; `c1335b8` failed-open/malformed-stream orphan quarantine regression, `6e0a52b` runner wiring; `da5147e` tests retry without ending context and exact-token failure/recovery. Existing `.github/workflows/platformio-build.yml` already runs `bash test/run_stream_test.sh` on PRs and includes all `src/**`/`test/**` paths, so no new approval-gated workflow was created. **No new test or build PASS was observed.** At `9498bcb` commit-linked PR workflow lookup was empty; at `6e0a52b` combined statuses were empty. The branch workflow history's displayed green run `35427506383` belongs to earlier SHA `7bffec3`, not this head.

## Remaining U1 acceptance / next work

1. **Priority blocker: move installed-class discovery/selection/probing/binding and actual `serial.port` device/stream publication into installed class/host providers with generic runtime admission.** `NativeUsbBridge` still loops candidates and holds serial sessions, `NativeUsbClassBridge` still binds ELF tables in firmware, and `NativeSerialPortBridge_implementation.inc`/`NativeUsbDeviceRegistry` still contain a firmware-originated USB-specific projection/stream path. The generic publisher is only a step toward replacement. Preserve arbitrary independently installed compatible classes, CDC/CP210x/CH34x, programmer/Serial Monitor, DTR/RTS, exact disconnect/reconnect and backpressure; ensure normal apps do not fall back to hardware bridges.
2. Reconcile master (119 master-only as compared on September 21) safely; implement exclusive debug-console/internal USB PHY handoff, checked restoration and quarantine after uncertain release.
3. Replace `NativeSerialPortBridge.cpp` parsed `USBREF`/`VBUSREF`/`USBCTRL` log diagnostics with actual structured provider error propagation. Safely migrate `usb-cdc-acm-v2` package identity to canonical `usb-cdc-acm` and retire linked legacy proxy.
4. Remove signing/P-256/floor per spec while preserving SHA/TLS/ABI/import/rollback; finish nested package resources and generation-bound verification receipts.
5. Obtain matching current-head firmware, Xtensa ELF, application, imports/relocations, packages/catalog and integration test evidence, including missing/corrupt package and charge/OTG/sleep where practical. All software implementation must precede owner-controlled hardware qualification. Native I²C/SPI/UART and unrelated peripherals belong to U3; CAM provisioning belongs to U4.

**Current evidence:** only commits and prior-head CI were directly observed. No present-head firmware/ELF/app/package or newly added host test PASS; no merge, tag, release, flash or owner hardware qualification. **Next production action:** replace compatibility-originated class binding/publication with installed provider-originated announcements and genuine generic runtime selection, then run linked production ELF/host-class integration tests. No intermediate hardware request.
