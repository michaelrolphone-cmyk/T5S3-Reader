# U1 implementation ledger

[PR #96](https://github.com/michaelrolphone-cmyk/T5S3-Reader/pull/96), `impl/u1-riscrte`, is the sole U1 implementation PR. Distinguish **committed source, observed tests and incomplete acceptance**. [USB remediation](USB_CONTRACT_VIOLATION_REMEDIATION.md), [execution order](FOUR_MILESTONE_STREAM_FIRST_EXECUTION_ORDER.md), [claim-scoped USB control](U1_USB_CONTROL_SCOPE_IMPLEMENTATION.md), [package identity policy](PACKAGE_IDENTITY_VERSION_POLICY.md), and `AGENTS.md` remain binding. The owner authorizes merge, tag, release, flashing and physical qualification.

## Ancestry and versions

U1 last backmerged master `1ebfbfe` at `0a8c21c`. September 20 comparison: master `839f66c` (firmware 1.2.48) had 119 master-only commits and U1 224 U1-only commits. Master #103 CH34x and #105 DMA/VBUS were selectively integrated; **not a full backmerge**. Remaining newer master work must be reconciled without discarding U1. U1 firmware 1.2.35 unreleased. App versions: Driver Manager 1.0.5, App Store 1.0.3, Package Manager 1.0.1. Current U1 manifest versions: board-power 0.1.6, controller 0.1.5 (master last compared 0.1.4), **usb-host-v2 0.1.4**, CDC 0.1.2, CP210x 0.1.3, CH34x 0.1.2. CDC forked `-v2` identity still needs explicit safe migration; a version bump is not identity migration.

## Generic runtime, packages and actual hardware ownership

- Generic ELF byte/record streams, context-owned grants and revocation, bounded partial RX/TX/backpressure, provider discovery and API-aware dependencies are committed. USB stop releases only its class/host grants, never global graph shutdown. Failed physical release retains exact mapped generations and teardown obligations.
- Only installed `i2c.bus` ELF may temporarily import bounded firmware raw I²C (native controller takeover belongs to U3). Installed BQ25896 `board.power.vbus` ELF alone owns charger/OTG/telemetry and one-way BATFET shutdown; board UI and sleep use verified grants, defer charger config until SD mount and reserve sleep ELF before SD shutdown. BQ27220 gauge remains separate. No second firmware BQ writer or absent-package hardware fallback. The BQ snapshot lacks REG12 charger-current ADC; no such measurement is claimed. Hardware tests unverified.
- Four-kind common package engine has per-ID recoverable SD/ZIP install, bounded bootstrap, SHA-pinned immutable catalog, shared App Store/Package Manager/Driver Manager operations, manifest-driven exporter and API-aware dependency order. Earlier isolated cross-translation-unit mutation ASan/UBSan smoke passed; full present-head build/rollback and corrupt-package acceptance remain unverified.

## Physical USB/controller/class source evolution

- `02ff428`–`fd94430`: append-only host `release_checked`, CDC/CP210x exact-session retention and bounded failed-open orphan cleanup. Closing claims deny I/O and remain retryable; source and host fixtures wired, unverified current head.
- `ca2c26f`–`5d9b5b5`: physical IDF-backed controller retains two-stage interface/device-close recovery and detached-handle reaping; isolated release-policy C++17 ASan/UBSan exercise passed earlier, not production linkage. `db75070`–`6b7b41b` selectively integrated master #105's bounded 500-ms bulk halt/flush/callback drain, ordering of DMA/interface/IDF host/VBUS cleanup and final IDF NO_CLIENTS event; uncertainty pins the provider and rail. Controller 0.1.5 remains target-link/hardware-unverified.
- `d534030`–`f63c2f8`: selectively integrated master #103's independently installable CH34x `serial.port` class: WCH VID/PID, descriptors, version/init, baud/framing, inverted DTR/RTS and bulk I/O are in ELF. Strengthened checked release, failed-close session retention and failed-open orphan retry. This chipset family is distinct from duplicate CDC package identity.
- `5eaa653`–`9d81322`: CP210x refreshes host-owned attach inventory before UART disable. Confirmed detach skips impossible control and releases same claim; unknown state, attached UART NACK and physical-release failure pin and retry. Orphan/stale-token tests wired.
- `e4c6739`–`cd8dc9a`: initial host device-control gate and physical-quiescence fault recovery still allowed a class-crossing legacy device-token call. `9e56af0`–`9c5790e` corrected it: legacy `host.control` now always rejects; append-only `control_claim` validates exact active nonclosing claim and present device, interface-recipient index, and device-recipient sole-device claim. CDC/CP210x/CH34x call with their OWN claim. Tests cover production host ELF and composite isolation; no green current-head run. Host was 0.1.3 at that stage; class versions CDC 0.1.2, CP210x 0.1.3, CH34x 0.1.2. [Full control contract and limits](U1_USB_CONTROL_SCOPE_IMPLEMENTATION.md).

## This continuation: host-owned snapshot and reconnect cleanup

- **`efadf13`** appends `risc_usb_device_identity_v1` and `risc_usb_host_snapshot_v1` after the unchanged host/discovery ABI prefix. It distinguishes physical presence from descriptor identification; no configuration-descriptor bytes are passed into the firmware compatibility bridge.
- **`b308b86`** implements snapshot entirely inside installed `usb.host`: poll at most 16 physical events, return bounded ordered generation-safe present-device tokens and validated VID/PID when available. A transient descriptor read failure returns the SAME present token with `identified=0`, not a fictitious detach. Capacity failure writes no partial records and reports required count; event loss fails closed and retains physical fault quarantine.
- **`23defff`** replaces firmware `NativeUsbBridge::reconcile` polling, device-list and 4-KiB descriptor retrieval with provider snapshot consumption. Its remaining class selection and legacy presentation state are NOT yet extracted. Also fixes `serialStop`'s quarantine early-return defect: quarantine blocks new I/O/acquisition, not retry of the SAME checked class close and host-grant release. If either fails, retain ownership and quarantine; only after both succeed clear quarantine and allow clean restart. This does not assert a hardware reconnect pass.
- **`b2e0c51`** adds a production-host ELF test for zero/limited-capacity snapshots, transient descriptor failure with retained identity, physical detach/replug renewal, event-fault fail-closed and verified controller quiescence recovery. **`ff62bba`** wires the test alongside existing host control and composite isolation tests. **`bab09c1`** updates source regression to guard against reintroducing firmware descriptor/event loops or skipping quarantined teardown retry. **`5942bc9`** bumps host package to **0.1.4**. Source and tests are COMMITTED, not observed passing on present head.

## Evidence and remaining U1 acceptance

Historic PlatformIO green `35427506383`, master #105 CI and earlier isolated sanitizer smokes precede this head. GitHub PR workflow lookup at `bab09c1` returned zero runs. Container GitHub DNS resolution failed during this continuation. No full present-head firmware, Xtensa USB/BQ ELF, host/class regression, app/package/catalog/import/relocation, or physical USB/charging/OTG/sleep pass is verified. No merge, tag, release, flash or owner hardware qualification occurred.

1. Move **remaining class discovery/probing/binding and semantic `serial.port` registry/stream publication** out of firmware USB orchestration into installed class/host providers and generic runtime. Host event/identity snapshots have been extracted, but `NativeUsbBridge` still chooses class candidates and maintains legacy USB-specific serial projection. Preserve CDC/CP210x/CH34x, new class installation without firmware edits, Serial Monitor/programmer DTR/RTS, reconnection and partial/backpressured I/O; safely retire normal resident USB fallback.
2. Reconcile master divergence; implement exclusive internal USB/debug-console PHY handoff, restore and uncertain teardown quarantine without regressing either controller branch.
3. Execute present-head BQ/USB firmware, Xtensa provider ELFs, host and app/package/catalog/import suites; validate missing/corrupt packages, charge/OTG exclusion, telemetry and both sleep paths, then owner-directed physical acceptance.
4. Replace parsed `USBREF`/`VBUSREF`/`USBCTRL` logs with structured errors, migrate CDC `usb-cdc-acm-v2` safely to canonical `usb-cdc-acm`, retire legacy proxy. Remove signing/P-256/floor but retain SHA/TLS/import/ABI/rollback; add nested resources and generation-bound verification receipts.
5. Obtain release-equivalent, single-head firmware/ELF/app/archive/catalog/test evidence. Native I²C/SPI/UART and unrelated peripherals belong to U3; CAM provisioning belongs to U4.

**Next production action:** provider-originated class binding and generic semantic publication, accompanied by current-head build feedback. No repetitive interim owner hardware tests.
