# U1 implementation ledger

[PR #96](https://github.com/michaelrolphone-cmyk/T5S3-Reader/pull/96), branch `impl/u1-riscrte`, is the sole implementation PR. Distinguish **committed source, observed test results and incomplete acceptance**. [USB remediation](USB_CONTRACT_VIOLATION_REMEDIATION.md), [four-milestone order](FOUR_MILESTONE_STREAM_FIRST_EXECUTION_ORDER.md), [claim-scoped USB control detail](U1_USB_CONTROL_SCOPE_IMPLEMENTATION.md), [package identity policy](PACKAGE_IDENTITY_VERSION_POLICY.md) and repository `AGENTS.md` remain binding. Only the owner authorizes merge, tag, release, flash or physical qualification.

## Ancestry and manifest versions

U1 last backmerged master `1ebfbfe` at `0a8c21c`. A September 20 comparison found master `839f66c` (firmware 1.2.48) 119 commits ahead while U1 had 224 independent commits; selective integration is **not** a full backmerge. Master PR #103 CH34x, #105 DMA/VBUS cleanup and subsequent work require careful preservation. U1 firmware 1.2.35 remains unreleased. App versions: Driver Manager 1.0.5; App Store 1.0.3; Package Manager 1.0.1. Current U1 driver manifest versions: `board-power-t5s3-v2` 0.1.6; `usb-controller-esp32s3` 0.1.5 (last compared master 0.1.4); `usb-host-v2` **0.1.3**; `usb-cdc-acm-v2` **0.1.2**; `usb-cp210x-v2` **0.1.3**; `usb-ch34x-v2` **0.1.2**. CDC's forked identity remains unresolved; never treat the version bump as a safe identity migration.

## Generic runtime, packages and hardware ownership

- Generic ELF byte/record streams, context-owned grants/revocation, partial bounded RX/TX/backpressure, provider discovery and API-aware dependency checks exist. USB stop (`3515ee0`–`a2035d4`) releases only its own class/host grants, not the entire provider graph. Failed physical release retains exact mapped generations and quiescence requirements.
- Only installed `i2c.bus` ELF temporarily imports bounded firmware raw I²C. Installed BQ25896 `board.power.vbus` ELF alone owns charger/OTG/telemetry and one-way BATFET shutdown; board UI and sleep use verified provider grants, defer config until SD mount and pre-reserve before SD sleep, with distinct BQ27220 gauge. No second firmware BQ register writer or absent-package hardware fallback. REG12 charger-current ADC is not in its snapshot, so no measurement may be invented. Board and hardware tests remain unverified.
- Four-kind ordinary package engine offers per-ID recoverable SD/ZIP installs, bounded ZIP bootstrap, immutable SHA-pinned catalog and shared normal operations across App Store/Package Manager/Driver Manager. Manifest-discovered exporter replaces a fixed seven-driver allowlist, with API-aware ordering. Cross-translation-unit mutation gate isolated ASan/UBSan smoke passed earlier, but full current build remains unverified.

## USB source evolution

- `02ff428`–`fd94430`: host append-only `release_checked` and class CDC/CP210x exact-session retention on failed release; a closing claim blocks data and remains retryable, failed-open orphan claims recover through bounded class quiescence. Tests wired, not verified green on this head.
- `ca2c26f`–`5d9b5b5`: physical IDF-backed controller keeps two-stage interface/device-close recovery and bounded reaping of detached unclaimed handles; isolated C++17 release-policy ASan/UBSan smoke passed earlier, not full controller linkage. `db75070`–`6b7b41b` selectively integrated master #105 500-ms bulk timeout halt/flush/callback drain, strict DMA/interface/IDF host/VBUS teardown ordering and the final IDF NO_CLIENTS event. Failed cleanup pins the provider/rail dependency. Physical controller manifest 0.1.5; target CI and board voltage remain unverified.
- `d534030`–`f63c2f8` selectively integrated master #103 CH34x independently installable `serial.port` ELF with vendor version/init, VID/PID and descriptor matching, baud/framing, inverted DTR/RTS and bulk I/O. It was strengthened for checked-release, failed-close retention and failed-open orphan recovery. It is a distinct WCH chipset family, not the duplicate CDC package identity.
- `5eaa653`–`9d81322`: CP210x refreshes provider-owned attach inventory before UART disable; a confirmed detach skips impossible control and releases exact claim while unknown state, attached NACK or physical-release failure remains pinned/retryable. Failed-open orphan and stale-token tests wired.
- `e4c6739`–`cd8dc9a`: first host device-control gate required a claim somewhere on the device and allowed faulted-host cleanup only after independent physical quiescence. It still exposed class-crossing authorization through the legacy public device token.
- **This continuation, `9e56af0`–`9c5790e`**: production host now disables legacy device-token `host.control()` and appends `control_claim()` after existing host/discovery/checked-release members. It verifies the caller's exact nonclosing live claim, present device, recipient/length/timeout, interface match for interface-recipient commands, and sole-device claim for CH34x device-recipient vendor commands. CDC, CP210x and CH34x ELFs all require the extension and pass their own claims, preserving CDC control/data separation, UART DTR/RTS and existing checked-release semantics. Host 0.1.3; CDC 0.1.2; CP210x 0.1.3; CH34x 0.1.2. Real host-ELF composite fixture checks independent interfaces, old device-token rejection, exclusive vendor controls, stale/closing tokens and failed-release quarantine. Existing host/CDC descriptor/CP210x/CH34x fixtures and mock 2-/3-ELF stack were adapted; `test/run_usb_host_v2_test.sh` wires the new test. [Detailed implementation and limitation](U1_USB_CONTROL_SCOPE_IMPLEMENTATION.md). **Tests are committed but NOT observed passing on this head.**

## Evidence and unfinished acceptance

Historic green PlatformIO run `35427506383`, master #105 green CI and earlier isolated release/mutation smoke all predate this head. GitHub's current-commit workflow query on `7a596f6` returned no PR workflow runs; no full present-head host suite, firmware, linked Xtensa USB/BQ ELF, app, import/relocation, package/catalog or hardware PASS is verified. Git checkout from the working container has previously failed DNS resolution. No merge, release, tag, flash or owner hardware qualification in this continuation.

Still blocking U1, in dependency order:

1. Move **USB device polling/configuration, arrivals/removals, class probe/bind and semantic `serial.port` device/stream publication out of compiled `NativeUsbBridge` into installed host/class ELFs and generic runtime**. Preserve CDC/CP210x/CH34x, Serial Monitor, ESP programmer DTR/RTS, reconnect, partial/backpressured I/O and future additional class drivers without firmware edits. Neither claim-scoped controls nor class teardown completes this core migration.
2. Reconcile remaining master ancestry; implement exclusive internal USB/debug-console PHY handoff, safe restore and failed-teardown quarantine without losing either branch's controller fixes.
3. Verify current firmware/ELF/app tests, imports/relocations, catalog and BQ behavior; then owner-directed physical charging, OTG, sleep, missing/corrupt package and USB qualification.
4. Replace parsed `USBREF`/`VBUSREF`/`USBCTRL` logs with structured provider errors; migrate forked `usb-cdc-acm-v2` safely to canonical `usb-cdc-acm` and retire legacy firmware proxy. Remove signing/P-256/floor without losing SHA/TLS/import/ABI/rollback; implement nested resources and generation-bound ELF verification receipts.
5. Produce matching release-equivalent firmware/ELF/app/archive/source/test evidence before final acceptance. Native I²C/SPI/UART and unrelated peripherals are U3; CAM provisioning is U4. Avoid interim owner hardware tests while implementing U1.
