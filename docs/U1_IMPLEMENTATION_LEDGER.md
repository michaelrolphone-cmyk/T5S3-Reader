# U1 implementation ledger

[PR #96](https://github.com/michaelrolphone-cmyk/T5S3-Reader/pull/96), `impl/u1-riscrte`, is the **only** U1 implementation branch/PR. [USB remediation](USB_CONTRACT_VIOLATION_REMEDIATION.md), [execution order](FOUR_MILESTONE_STREAM_FIRST_EXECUTION_ORDER.md), [claim-scoped USB control](U1_USB_CONTROL_SCOPE_IMPLEMENTATION.md), [package identity](PACKAGE_IDENTITY_VERSION_POLICY.md), and `AGENTS.md` remain binding. Distinguish committed code from observed build/test results. Owner controls merge, tag, release, flash and physical qualification.

## Ancestry and versions

September 21 comparison at `6e0a52b`: master `839f66c` had **119 master-only commits**, U1 had 283 branch-only commits then. Subsequent U1 commits below followed. Last full master backmerge `1ebfbfe` at `0a8c21c`. Master #103 CH34x and #105 DMA/VBUS were **selectively** incorporated, not fully backmerged. Master firmware was 1.2.48 at that comparison; U1 firmware 1.2.35 remains unreleased. U1 apps: Driver Manager 1.0.5, App Store 1.0.3, Package Manager 1.0.1. Drivers: board-power 0.1.6, USB controller 0.1.5, host 0.1.4, CDC 0.1.2, CP210x 0.1.3, CH34x 0.1.2. CDC `-v2` identity still requires safe migration; no driver manifest or ELF version was changed in this continuation.

## Previous committed implementation

- Generic ELF byte/record streams, execution-context-owned leases/revocation and partial RX/TX/backpressure. Recoverable four-kind per-ID SD/ZIP package engine, bounded bootstrap, immutable SHA-pinned catalog, shared App Store/Driver Manager/Package Manager and manifest-driven exporter. Prior isolated mutation sanitizer smoke is not present-head evidence.
- Installed `i2c.bus` ELF is the ONLY authorized temporary bounded raw firmware I²C importer until U3; installed `board.power.vbus` ELF owns BQ25896 charger/OTG/VBUS/telemetry and BATFET shutdown, with UI/sleep grants and SD/startup ordering. BQ27220 remains separate. No REG12 charger-current ADC claim; hardware not qualified.
- `02ff428`–`fd94430`: host checked release and CDC/CP210x session/orphan retention. `ca2c26f`–`5d9b5b5`: controller two-stage interface/device close and detached reap. `db75070`–`6b7b41b`: selectively incorporated master #105 bounded 500-ms bulk DMA halt/flush/callback drain, IDF/VBUS shutdown ordering and uncertain-cleanup quarantine.
- `d534030`–`f63c2f8`: independent CH34x class ELF with actual WCH parsing, vendor requests, baud/framing, DTR/RTS and bulk transfers. `5eaa653`–`9d81322`: CP210x confirmed-detach versus unknown-state close handling. `9e56af0`–`9c5790e`: legacy host device-token control fail-closed; append-only claim-scoped control isolates interface and sole-claim device vendor requests. CDC/CP210x/CH34x use their own claims. Historic release-policy sanitizer smoke is NOT a target ELF or present-head test result.
- Through `bcd89c1`: `efadf13` append-only `risc_usb_host_snapshot_v1`; `b308b86` installed-host bounded event/identity snapshot, transient descriptor failures distinguish from detach; `23defff` firmware reconcile consumes snapshot rather than direct poll/device list/config descriptors and retries checked class/host cleanup during quarantine; `b2e0c51`, `ff62bba`, `bab09c1` commit host snapshot/teardown regressions and runner; `5942bc9` host 0.1.4. Compiled legacy class binding and presentation remain.

## September 21: generic publisher, selection and acquisition recovery

- `f7d8a90`, `e15799c`: transport-neutral generation-checked semantic device publisher, stale token and conflicting metadata rejection, checked withdrawal. `6fa11df` transitions the legacy `UsbSerialProjection` implementation onto it. `e1a685a`, `9498bcb` commit publisher tests/runner. This is NOT yet provider-originated publication.
- `352fd72`, `affff67`: generic serial registry retains a private exact lease on failed/malformed acquire when its physical release fails, with no public lease/streams exposed; context shutdown or next acquisition retries the SAME checked release and denies reconnect until success. `c1335b8`, `6e0a52b`, `da5147e` commit reconnect/regression tests and runner.
- `0504f3d`, `990884a`: hardware-blind manifest/capability/API candidate enumeration, exact acquire, checked rejected candidate release through generic `InstalledProviderSelector`. Class matching still occurs in each installed ELF's `open(device)`. `8b1ae48` exposes an unbound retained class grant to checked compatibility `serialStop`. `594d63c`, `952a56e`, `5f9b959` commit source/probe/fault tests and provider graph runner. Earlier ledger/PR descriptions updated at `59deb28`.

## September 21 continued: exact grant ownership and production host/class test

- **`044bc89`** fixes `RuntimeInstalledProviders::acquire()`: if the graph grants an ELF but its interface is unavailable, a FAILED checked release now returns the exact generation-bound grant (null interface) to the caller instead of orphaning it. A successful release clears it; failure never authorizes I/O.
- **`a31dd84`**, **`3007746`** introduce generic hardware-blind `InstalledProviderSession` and use it in the compiled class compatibility adapter. Selection and rejected-grant ownership reside in the generic runtime; failed activation **without a returned grant** remains quarantined rather than being cleared by a no-op unbind. Exact-grant release is retryable, and normal class stop, streams, line coding and control operations remain connected. This is an intermediate extraction, not the final provider-originated class binding.
- **`aaee82f`**, **`c7603d5`** add and wire a generic session fault regression: accepted class, checked rejected-class release retry, failed interface acquisition with retained grant, fail-without-grant quarantine, and prevention of selecting another class while uncertain.
- **`fa9f579`**, **`9eb5411`** prevent a failed USB host acquisition from overwriting a retained grant. `serialStop()` can retry its exact release. A subsequent `serialStart()` cannot overwrite it, while an already-running session can still change line coding. `9eb5411` corrects an overbroad guard in the preceding commit; use the latter as the final implementation.
- **`3026b38`**, **`6473517`** commit and wire a host/class ELF integration test to the pre-existing `test/run_usb_host_v2_test.sh`: dynamically links the production installed host plus CDC, CP210x and CH34x ELFs against a simulated controller, exercises real descriptor matching, claim-scoped control, line coding, DTR/RTS, bounded partial reads/writes, failed physical release retention/retry and generation change on reconnect. Test code is committed, but **no observed PASS** has been obtained. No new approval-requiring workflow was created.

## Evidence and incomplete acceptance

The historical successful workflow `35427506383` was for older SHA `7bffec3` and cannot qualify current code. GitHub lookup for current `6473517` returned **zero pull-request workflow runs** and zero combined statuses. Container checkout failed because GitHub DNS could not resolve, and no repository exists in the available working container; therefore no full current-head local test/build, firmware/Xtensa ELF, app/package/catalog/import/relocation or new integrated host/class PASS is claimed. No merge, tag, release, flash or hardware qualification occurred.

1. **Primary blocker:** move the remaining installed class *per-device selection/probing/binding* and semantic `serial.port` device/stream publication out of compiled `NativeUsbBridge`, `NativeUsbClassBridge`, `NativeSerialPortBridge_implementation.inc`/USB-specific registry and into installed providers with truly generic runtime. Generic candidate enumeration/ownership is extracted but the compiled legacy bridge still loops devices/classes and handles connection state and stream shuttling. Support newly installed compatible classes without firmware edits, real Serial Monitor/ESP programmer line coding/DTR/RTS, safe disconnect/reconnect and partial/backpressured I/O. Add tests validating provider-originated publication and actual consumer path. Do not disguise a firmware helper as an ELF.
2. Reconcile remaining master divergence; implement exclusive internal debug-console/USB PHY handoff, checked restoration and quarantine on uncertain shutdown.
3. Replace `USBREF`/`VBUSREF`/`USBCTRL` log scraping with structured provider errors. Safely migrate forked `usb-cdc-acm-v2` to canonical `usb-cdc-acm` and retire the legacy proxy.
4. Remove signing/P-256/floor while retaining SHA/TLS/ABI/import/rollback; finish nested resources and generation-bound ELF verification receipts.
5. Obtain single-head firmware, Xtensa providers, applications, imports/relocations, packages/catalog, missing/corrupt install and charging/OTG/sleep lifecycle build/test evidence; owner-directed hardware acceptance comes afterward. Native I²C/SPI/UART/unrelated peripherals belong to U3; CAM provisioning belongs to U4.

**Next implementation:** installed-provider-owned class binding and semantic publication cutover, followed by actual tests/builds. **Implementation In Progress.**
