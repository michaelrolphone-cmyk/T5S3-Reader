# U1 implementation ledger

[PR #96](https://github.com/michaelrolphone-cmyk/T5S3-Reader/pull/96), `impl/u1-riscrte`, is the sole implementation PR. Distinguish **committed source, observed checks and incomplete acceptance**. [USB remediation](USB_CONTRACT_VIOLATION_REMEDIATION.md), [execution order](FOUR_MILESTONE_STREAM_FIRST_EXECUTION_ORDER.md) and repository `AGENTS.md` remain binding. Owner authorizes merge, tags, release, flashing and physical qualification.

## Branch ancestry and version lineage

U1 last backmerged master `1ebfbfe` at `0a8c21c`; branch firmware 1.2.35 is unreleased. Compared September 20, 2026 master `839f66c` (firmware 1.2.48): at comparison time master was 119 commits ahead and this PR had 224 independent commits. The branches remain divergent, so **selective integration is not a master backmerge**. Master already contains #103 CH34x and #105 DMA/VBUS teardown, plus unrelated newer runtime/controller code; reconcile remaining differences without overwriting U1 checked-release/BQ or master improvements. Current changed app versions: Driver Manager 1.0.5, App Store 1.0.3, Package Manager 1.0.1. Board power `board-power-t5s3-v2` is 0.1.6; USB host and CDC are 0.1.1; CP210x is **0.1.2**; controller is **0.1.5** (above master's 0.1.4); imported/modified `usb-ch34x-v2` is **0.1.1** (above master's 0.1.0). These are manifest package versions; no new firmware or package was released.

## Generic runtime and installed hardware ownership

- Generic ELF byte/record streams, context-owned capability grants/revocation, bounded partial RX/TX and backpressure, verified provider discovery and API-aware dependency checks exist. USB stop (`3515ee0`–`a2035d4`) releases its own class/host grants, never shuts down the entire provider graph. Failure retains/quarantines exact module generations.
- The installed `i2c.bus` ELF alone may import the temporary bounded firmware I²C controller adapter. BQ25896 charger/USB power, physical IDF USB controller, USB host, CDC, CP210x and CH34x device-class implementations belong in their installable ELFs, not hardware-specific firmware drivers. Generic app target is `serial.port`, not `open_usb`.
- Host/class checked-release (`02ff428`–`fd94430`) appends `release_checked` to host discovery ABI without changing its initial prefix. A failed physical interface release pins its claim, disallows transfers, preserves class sessions, and retries failed-open orphans without closing other consumers. Host/CDC/CP210x regressions committed and wired; no full runner PASS observed at this head.
- Physical controller (`ca2c26f`–`5d9b5b5`) retains a two-stage `ClaimReleasePolicy.h`: a successful interface release followed by failed detached-device close retains the exact token and retries only unfinished close, never releases the same interface twice; unclaimed detached device handles are reaped by a bounded table. One isolated reconstructed C++17 ASan/UBSan policy exercise passed in an earlier continuation, **not** the full repository runner.
- **Selective master #105 integration (`db75070`, `e797bcc`, `e74ef8c`, `acdcbd9`, `6b7b41b`):** the real controller ELF bounds bulk timeout cleanup to a 500-ms halt/flush/callback drain; verifies a transfer belongs to the interface before release, preserves staged claim retry and drains DMA before freeing transfers, uninstalling the host or releasing VBUS. Undrainable bulk/control operations fail closed and remain pinned. It consumes IDF's final NO_CLIENTS event even when no device attached and permits verified faulted-state cleanup. A structural source regression is wired in the USB host runner, and an obsolete regression requiring global graph shutdown was corrected. This is **not verified by Xtensa build or physical measurement**.
- **Selective master #103 CH34x integration (`d534030`–`f63c2f8`):** imported actual chipset VID/PID matching, descriptor parser, WCH vendor control/baud/framing, DTR/RTS, bulk RX/TX and lifecycle as an independently installable `serial.port` ELF. Unlike the current master 0.1.0 implementation, U1 0.1.1 requires checked host ABI, retains its session on failed close, quarantines failed-open orphan claims for bounded quiescence retry, and rejects legacy void-release-only hosts. Stable package identity `usb-ch34x-v2` is preserved; manifest builder, host protocol regression, existing USB runner and experimental CI integrate this provider without a fixed catalog whitelist. **Builds/tests are committed and wired, not observed passing.** CH34x is a separate chipset class, not a duplicate canonical CDC identity.
- **CP210x confirmed-detach release (`5eaa653`, `b47ee80`, `9d81322`):** the installed CP210x ELF now requires provider-owned `usb.host` poll/devices and checked-release functions. Before disabling UART on close, it polls host events and checks the generation-bound device inventory. A confirmed detach skips the impossible vendor control and uses `release_checked` to relinquish the exact interface claim; an unsuccessful poll, invalid inventory or failed disable while still attached retains the claim. A later failed physical release retains the disabled stage and exact session for retry, with no duplicate disable. Failed-open orphans can also be released after confirmed unplug. Manifest `usb-cp210x-v2` 0.1.1 → **0.1.2**. Production code and executable mock-controller host regression for missing discovery, unknown state, attached NACK, detach, failed checked release, orphan recovery and stale tokens committed. The existing `test/run_usb_cp210x_v2_test.sh` runs the updated test, **but no execution/CI result was observed for these commits**. This resolves a concrete detach cleanup defect, not the larger firmware-orchestration cutover.

## BQ single-owner cutover and package engine

- `board.power.vbus` ELF owns all BQ25896 telemetry, charge profile (1000 mA input, 512 mA charge, 64 mA precharge/termination, 4208 mV regulation, 3300 mV SYS_MIN), USB OTG and one-way BATFET shutdown, with readback and uncertain-write quarantine. `ff173dd`–`17695f6` removed the board resident BQ HAL writer, replacing it with verified provider leases for charger/UI/shutdown, while retaining independent BQ27220 gauge. Configuration waits for post-SD mount, with bounded retries. Before SD deinit, display sleep reserves the provider for shutdown; failed reservation never loads from disconnected SD. No fallback writer. Board source/charge/shutdown tests are wired, target builds and physical behavior unverified; REG12 charger-current ADC is absent from snapshot, so no invented measurement.
- Four-kind ordinary package engine supports per-ID recoverable SD/ZIP installs, bounded ZIP bootstrap, immutable-release hash-pinned generic catalog and unified app/driver/service/provider API. App Store, Driver Manager and Package Manager normal operations use it; historical stage recovery retained. Manifest discovery replaces fixed seven-provider exporter; dependency and identity checks are API aware. `f24c02c`–`484a46c` install/recovery cross-TU mutation gate isolated two-TU ASan/UBSan smoke passed earlier. Full system build has not been observed.

## Evidence and remaining acceptance

Historic green PlatformIO `35427506383` and earlier USB runs predate these changes; a strict warning precedence smoke and isolated gate/claim-policy smokes passed earlier, not full repository tests. This turn's container could not resolve github.com to check out the repo; direct workflow lookup for the CP210x test commit returned **no matching run**. Current firmware, linked controller/CH34x/CP210x/BQ ELFs, host source tests, class regressions, package/catalog/import audit and physical USB/charge/OTG/VBUS/sleep behavior are **not verified at this head**. CI success on merged master PR #105 does not prove this integrated branch. No merge, tag, release, flash or owner test was performed.

Still blocking U1, in dependency order:

1. Move USB discovery/polling, configuration, arrival/removal, class probe/bind and semantic `serial.port` registry/stream publication **out of firmware `NativeUsbBridge` and into installed host/class ELFs**, preserving CDC/CP210x/CH34x, Serial Monitor/programmer DTR/RTS, reconnect and partial/backpressured I/O, and adding future classes without firmware changes.
2. Reconcile the **remaining** master commits, especially controller/USB lifecycle and generic runtime changes, without losing branch features. Implement exclusive USB debug-console/internal PHY handoff in platform port/controller and quarantine failed handoff. Do not mistake selective #103/#105 integration for complete ancestry reconciliation.
3. Obtain current BQ and USB target build/link/source-test feedback and fix actual failures; verify missing/corrupt package, source exclusion, UI battery metrics and both sleep paths before qualifying hardware.
4. Replace scraped `USBREF`/`VBUSREF`/`USBCTRL` with structured provider errors; safely migrate obsolete `usb-cdc-acm-v2` fork into canonical `usb-cdc-acm`, retire reachable legacy firmware/proxy and preserve older generations/user data. Remove signing/P-256/floor scope while keeping SHA/TLS/ABI/import/rollback, add nested package resources and generation-bound ELF validation receipts.
5. Verify present-head firmware/ELF/app/host/archive builds, package catalog and imports; owner controls eventual hardware/release qualification. Native I²C/SPI/UART replacement and unrelated peripherals are U3; CAM provisioning is U4 and cannot substitute for U1 USB work.

**Next source action:** finish provider-originated discovery and reconcile still-divergent master implementation safely; respond to real CI feedback rather than demand repeated owner hardware testing.
