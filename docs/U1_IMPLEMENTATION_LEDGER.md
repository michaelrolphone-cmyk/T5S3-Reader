# U1 implementation ledger

[PR #96](https://github.com/michaelrolphone-cmyk/T5S3-Reader/pull/96), `impl/u1-riscrte`, is the **only** U1 implementation PR/branch. [USB remediation](USB_CONTRACT_VIOLATION_REMEDIATION.md), [execution order](FOUR_MILESTONE_STREAM_FIRST_EXECUTION_ORDER.md), [claim-scoped USB control](U1_USB_CONTROL_SCOPE_IMPLEMENTATION.md), [package identity](PACKAGE_IDENTITY_VERSION_POLICY.md), and `AGENTS.md` remain binding. Distinguish **written/committed code from observed build/test results**. Owner controls merge, tag, release, flash, physical qualification.

## Ancestry and versions

Last actual comparison on September 21 at `6e0a52b`: master `839f66c` has **119 master-only commits**, U1 had 283 branch-only at that point (additional commits below followed). Last full master backmerge `1ebfbfe` at `0a8c21c`. Master PR #103 CH34x and #105 DMA/VBUS were **selectively** integrated, not fully backmerged. Master was firmware 1.2.48 at comparison; U1 firmware 1.2.35 remains unreleased. U1 app versions: Driver Manager 1.0.5, App Store 1.0.3, Package Manager 1.0.1. Provider versions board-power 0.1.6, controller 0.1.5, host 0.1.4, CDC 0.1.2, CP210x 0.1.3, CH34x 0.1.2. Forked CDC `-v2` package identity still needs safe canonical migration; no provider ELF or package manifest was edited in this continuation.

## Previously committed software, physical ownership and limitations

- Generic ELF byte/record stream APIs, context-owned grants/revocation, partial RX/TX/backpressure, provider discovery/dependencies. Four-kind recoverable per-ID SD/ZIP package manager, bounded bootstrap, immutable SHA-pinned catalog, common App Store/Driver Manager/Package Manager and manifest-driven exporter. Earlier isolated package mutation sanitizer smoke passed, not a present-head pass.
- The installed `i2c.bus` ELF holds the ONLY temporary bounded raw firmware I²C import (native controller takeover belongs to U3). The installed `board.power.vbus` ELF owns BQ25896 charger/OTG/VBUS/telemetry and BATFET shutdown; BQ27220 gauge remains separate. UI/sleep use verified grants with SD/startup ordering. No REG12 charger-current ADC measurement is claimed. Physical behavior not qualified.
- `02ff428`–`fd94430`: host checked release, CDC/CP210x exact session retention and orphan retries. `ca2c26f`–`5d9b5b5`: controller two-stage interface/device teardown and detached reap; earlier isolated release-policy sanitizer smoke, NOT target ELF evidence. `db75070`–`6b7b41b`: master #105 bounded 500ms USB bulk DMA halt/flush/callback drain, IDF/VBUS teardown ordering and quarantine on uncertain physical cleanup.
- `d534030`–`f63c2f8`: independently installed CH34x class ELF, WCH matching, baud/framing, inverted DTR/RTS, bulk transfers and checked teardown. `5eaa653`–`9d81322`: CP210x refresh/confirmed-detach/unknown-state retry. `9e56af0`–`9c5790e`: host's legacy device-token `control()` rejects requests; appended `control_claim` enforces generation, active own claim, interface scope and exclusive device recipient; CDC/CP210x/CH34x use only their own claims. Existing production host/composite tests are COMMITTED; no current-head green observed.

## Prior continuation through `bcd89c1`: host snapshot

`efadf13` appends `risc_usb_host_snapshot_v1` and generation-token identity API without disturbing ABI prefix. `b308b86` implements installed-host bounded polling (up to 16 events), coherent presence/VID/PID identity, transient descriptor failure not detach, capacity failure without partial output, event loss fail-closed. `23defff` changes `NativeUsbBridge::reconcile()` to consume that ELF snapshot, removing firmware direct poll/device-list/config retrieval/4-KiB descriptor loop; legacy class selection and serial UI remain compiled. It fixes `serialStop()` to retry exact checked class close and host release in quarantine and clear quarantine only after both succeed. `b2e0c51`, `ff62bba`, `bab09c1` commit snapshot/teardown tests and runner/source checks; `5942bc9` host manifest 0.1.4. No present-head test PASS.

## September 21 continuation: production code and tests (committed)

**Generic semantic publication:** `f7d8a90` introduces `src/runtime/capabilities/ProviderDevicePublisher.h`: transport-agnostic generation identity, descriptor-based semantic publication, exact registry revocation and retained removal on failure. `e15799c` rejects stale-generation resurrection or metadata conflict without silently replacing live leases. `6fa11df` makes the existing transitional `UsbSerialProjection` delegate to that generic publisher. This DOES NOT move event announcements out of firmware or retire the firmware USB-specific registry. Publisher tests `e1a685a` and stream runner wiring `9498bcb` committed.

**Failed acquisition / reconnect:** `352fd72` modifies generic `RuntimeSerial::Registry`: failed or malformed acquisition with nonzero private token and failed physical release retains the private lease with no public handle or streams; rejects conflicting acquisitions/unregistration; `end()` retries exact release. `affff67` permits a later acquisition to retry that same orphan release once, admitting reconnect only after verified success. Tests `c1335b8`, runner wiring `6e0a52b` and expanded reconnect recovery `da5147e` committed. Prior successful public lease behavior retained.

**Manifest-driven candidate selection:** `0504f3d` introduces `src/runtime/drivers/InstalledProviderSelector.h`. Its generic, hardware-blind `selectNext()` enumerates *installed manifest capabilities* by requested ID/API, acquires the precise package grant, invokes a caller's versioned-interface acceptance callback, performs checked rejection, and stops on failed acquire/probe/release retaining any available exact grant; no CDC/CP210x/CH34x/VID allowlist. `990884a` connects `NativeUsbClassBridge` candidate enumeration and rejected-grant handling to the generic selector; individual class ELF `.open(device)` remains responsible for device matching. `594d63c` updates source-boundary regression; `952a56e` tests simulated first-reject/second-match, exhaustion, failed rejection cleanup, probe and acquire faults; `5f9b959` wires the production-header selector test into existing `test/run_provider_graph_v2_test.sh`. `8b1ae48` ensures a retained class grant without a bound stream table is visible to existing compatibility `serialStop()` checked cleanup; otherwise that fault path would strand a hidden class grant. **This moves generic candidate enumeration but NOT full class open/selection/binding or semantic stream publication out of the compiled compatibility bridge.** No independent installed-provider class coordinator exists yet. Class drivers remain independently installable and no hardcoded class list was introduced.

`bd24606` was the first ledger update; this continuation updated the existing PR description without opening another PR. No existing workflow that requests manual approvals was created/altered. `.github/workflows/platformio-build.yml` already triggers on relevant `src/**`/`test/**` pull request changes and runs `bash test/run_stream_test.sh`; `.github/workflows/usb-elf-provider-v2.yml` already calls `test/run_provider_graph_v2_test.sh`. New test commits are **not PASS evidence**.

## Observed evidence / acceptance blockers

GitHub workflow lookup returned **zero runs** for SHA `9498bcb`, SHA `bd24606`, and current code SHA `8b1ae48`; combined commit statuses for `6e0a52b` were empty. Historical successful workflow `35427506383` executed on older `7bffec3`, NOT this head. No matching present-head firmware, Xtensa provider ELF, production USB host/class integration, app/package/catalog/imports/relocations or added unit test PASS. Container GitHub DNS resolution failed earlier, so no complete branch checkout/local build was observed. No merge, tag, release, flash or owner hardware qualification was performed.

1. **Primary architectural acceptance:** installed ELF/provider-owned class discovery, per-device probe/open/selection/binding, publication of `serial.port` semantic device + stream endpoints and generic runtime consumer path. Compiled `NativeUsbBridge` still iterates device/class binding, compiled `NativeUsbClassBridge` still binds class ops and relays I/O, and `NativeSerialPortBridge_implementation.inc` plus `NativeUsbDeviceRegistry` still originate USB-specific publication. Remove that orchestration from firmware without moving it into another firmware helper or reviving resident fallback. Preserve CDC/CP210x/CH34x, arbitrary future compatible class ELFs, identity, checked disconnect/reconnect, DTR/RTS, partial/backpressured Serial Monitor/programmer I/O. Integrate real host/class ELF simulated hardware tests.
2. Master divergence safely backmerge/reconcile; exclusive internal USB/debug-console PHY ownership, release/restore and uncertain-teardown quarantine.
3. Replace USBREF/VBUSREF/USBCTRL log scraping with structured provider errors, safely migrate `usb-cdc-acm-v2` to canonical `usb-cdc-acm` and retire proxy.
4. Remove signing/P-256/floor while retaining SHA/TLS/ABI/import/rollback; finish nested resources and generation-bound verification receipts.
5. Present-head firmware/Xtensa ELFs/apps/imports/relocations/packages/catalog integrity, missing/corrupt package, charge/OTG/sleep lifecycle tests. Hardware qualification only after owner-directed completed software. Native I²C/SPI/UART and unrelated peripherals are U3, CAM provisioning is U4.

**Next source action:** installable provider-owned class coordinator and semantic publication replacing the still-compiled USB-specific class/session path, not another description or stand-alone test. **Implementation In Progress.**
